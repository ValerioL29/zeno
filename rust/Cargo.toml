[package]
name = "zeno"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1", features = ["sync"] }

[dev-dependencies]
tokio = { version = "1", features = ["rt-multi-thread", "macros"] }